/*
Future import idea:
This file is a placeholder for a build script that can read /database/resources/*.json
and compile them into the live /data/*.js files.

For now, the site still works without a build step.
*/
